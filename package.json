{
  "name": "firstApp",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://jeffpash.github.io/deploying-create-react-app",
  "devDependencies": {
    "gh-pages": "^2.0.1",
    "react-scripts": "^3.0.1"
  },
  "dependencies": {
    "react": "^15.4.2",
    "react-dom": "^15.4.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "deploy": "npm run build&&gh-pages -d build"
  }
}
